*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --blue: #1D4ED8;
  --blue-light: #EFF6FF;
  --blue-mid: #DBEAFE;
  --bg: #F1F5F9;
  --surface: #FFFFFF;
  --text: #0F172A;
  --text-muted: #64748B;
  --text-faint: #94A3B8;
  --border: #E2E8F0;
  --border-strong: #CBD5E1;
  --success: #16A34A;
  --success-bg: #F0FDF4;
  --success-border: #BBF7D0;
  --warning: #D97706;
  --warning-bg: #FFFBEB;
  --warning-border: #FDE68A;
  --danger: #DC2626;
  --danger-bg: #FEF2F2;
  --danger-border: #FECACA;
  --radius: 12px;
  --radius-sm: 8px;
  --radius-xs: 6px;
  --shadow: 0 1px 3px rgba(0,0,0,.06), 0 1px 2px rgba(0,0,0,.04);
  --shadow-md: 0 4px 12px rgba(0,0,0,.08), 0 2px 4px rgba(0,0,0,.04);
  --shadow-lg: 0 10px 40px rgba(0,0,0,.12), 0 4px 8px rgba(0,0,0,.06);
  --shadow-xl: 0 20px 60px rgba(0,0,0,.15), 0 8px 20px rgba(0,0,0,.08);
}

body {
  font-family: 'DM Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  font-size: 15px;
  line-height: 1.6;
  min-height: 100vh;
}

/* ===================================
   LOGIN PAGE
=================================== */
#loginPage {
  display: none;
  min-height: 100vh;
  background: linear-gradient(135deg, #0f172a 0%, #1e3a5f 40%, #1D4ED8 100%);
  position: relative;
  overflow: hidden;
}
#loginPage.active { display: flex; align-items: center; justify-content: center; padding: 24px; }

/* Decorative background shapes */
.login-bg-blob {
  position: absolute;
  border-radius: 50%;
  filter: blur(60px);
  opacity: 0.18;
  pointer-events: none;
}
.login-bg-blob-1 {
  width: 500px; height: 500px;
  background: #60a5fa;
  top: -150px; right: -100px;
}
.login-bg-blob-2 {
  width: 400px; height: 400px;
  background: #818cf8;
  bottom: -120px; left: -80px;
}
.login-bg-blob-3 {
  width: 300px; height: 300px;
  background: #34d399;
  top: 40%; left: 55%;
  opacity: 0.10;
}

/* Dot grid overlay */
#loginPage::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image: radial-gradient(circle, rgba(255,255,255,.08) 1px, transparent 1px);
  background-size: 28px 28px;
  pointer-events: none;
}

/* Split layout */
.login-split {
  display: flex;
  width: 100%;
  max-width: 980px;
  min-height: 580px;
  border-radius: 20px;
  overflow: hidden;
  box-shadow: var(--shadow-xl);
  position: relative;
  z-index: 1;
  animation: loginFadeIn .5s cubic-bezier(.4,0,.2,1);
}
@keyframes loginFadeIn {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* Left branding panel */
.login-brand {
  width: 42%;
  background: linear-gradient(160deg, rgba(29,78,216,.9) 0%, rgba(15,23,42,.95) 100%);
  padding: 52px 44px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  position: relative;
  flex-shrink: 0;
}
.login-brand::after {
  content: '';
  position: absolute;
  inset: 0;
  background-image: radial-gradient(circle, rgba(255,255,255,.06) 1px, transparent 1px);
  background-size: 22px 22px;
}
.brand-top { position: relative; z-index: 1; }
.brand-logo {
  display: flex; align-items: center; gap: 10px;
  font-weight: 700; font-size: 20px; color: #fff;
  margin-bottom: 48px;
}
.brand-logo-icon {
  width: 38px; height: 38px;
  background: rgba(255,255,255,.15);
  border: 1px solid rgba(255,255,255,.2);
  border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  backdrop-filter: blur(4px);
}
.brand-logo-icon svg { width: 20px; height: 20px; }
.brand-tagline {
  font-size: 26px; font-weight: 600;
  color: #fff; line-height: 1.3;
  margin-bottom: 16px;
}
.brand-tagline span {
  background: linear-gradient(90deg, #93c5fd, #c4b5fd);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.brand-desc { font-size: 14px; color: rgba(255,255,255,.6); line-height: 1.6; }
.brand-features { position: relative; z-index: 1; }
.brand-feat {
  display: flex; align-items: center; gap: 12px;
  padding: 10px 0;
  border-bottom: 1px solid rgba(255,255,255,.08);
  color: rgba(255,255,255,.75);
  font-size: 13px;
}
.brand-feat:last-child { border-bottom: none; }
.brand-feat-icon {
  width: 28px; height: 28px;
  background: rgba(255,255,255,.1);
  border-radius: 7px;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
  color: #93c5fd;
}
.brand-feat-icon svg { width: 14px; height: 14px; }
.brand-bottom {
  position: relative; z-index: 1;
  font-size: 12px; color: rgba(255,255,255,.35);
}

/* Right form panel */
.login-form-panel {
  flex: 1;
  background: var(--surface);
  padding: 52px 48px;
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.login-form-header { margin-bottom: 32px; }
.login-form-title {
  font-size: 22px; font-weight: 600; color: var(--text);
  margin-bottom: 6px;
}
.login-form-sub { font-size: 14px; color: var(--text-muted); }

/* Role Switcher */
.role-switcher {
  display: flex;
  background: var(--bg);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 4px;
  gap: 4px;
  margin-bottom: 28px;
}
.role-btn {
  flex: 1;
  padding: 9px 12px;
  border: none;
  border-radius: 6px;
  font-family: 'DM Sans', sans-serif;
  font-size: 14px; font-weight: 500;
  cursor: pointer;
  transition: all .2s;
  background: transparent;
  color: var(--text-muted);
  display: flex; align-items: center; justify-content: center; gap: 7px;
}
.role-btn svg { width: 15px; height: 15px; }
.role-btn.active {
  background: var(--surface);
  color: var(--blue);
  box-shadow: var(--shadow);
}
.role-btn:focus { outline: 2px solid var(--blue); outline-offset: 2px; }

/* Form Fields */
.login-field { margin-bottom: 18px; }
.login-field label {
  display: block;
  font-size: 13px; font-weight: 500;
  color: var(--text-muted);
  margin-bottom: 7px;
}
.login-input-wrap { position: relative; }
.login-input-icon {
  position: absolute;
  left: 13px; top: 50%; transform: translateY(-50%);
  color: var(--text-faint);
  pointer-events: none;
  display: flex;
}
.login-input-icon svg { width: 16px; height: 16px; }
.login-input {
  width: 100%;
  padding: 11px 14px 11px 42px;
  border: 1.5px solid var(--border);
  border-radius: var(--radius-xs);
  font-family: 'DM Sans', sans-serif;
  font-size: 14px; color: var(--text);
  background: var(--bg);
  outline: none;
  transition: border-color .15s, background .15s, box-shadow .15s;
}
.login-input:focus {
  border-color: var(--blue);
  background: #fff;
  box-shadow: 0 0 0 3px rgba(29,78,216,.08);
}
.login-input.error-state {
  border-color: var(--danger);
  background: var(--danger-bg);
}
.login-input.error-state:focus {
  box-shadow: 0 0 0 3px rgba(220,38,38,.08);
}
.pw-toggle {
  position: absolute;
  right: 12px; top: 50%; transform: translateY(-50%);
  background: none; border: none; cursor: pointer;
  color: var(--text-faint); padding: 4px;
  display: flex; align-items: center;
  border-radius: 4px; transition: color .15s;
}
.pw-toggle:hover { color: var(--text-muted); }
.pw-toggle:focus { outline: 2px solid var(--blue); outline-offset: 2px; }
.pw-toggle svg { width: 16px; height: 16px; }

/* Error message */
.login-error {
  display: none;
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  background: var(--danger-bg);
  border: 1px solid var(--danger-border);
  border-radius: var(--radius-xs);
  font-size: 13px;
  color: var(--danger);
  margin-bottom: 16px;
  animation: shakeX .4s cubic-bezier(.36,.07,.19,.97);
}
.login-error.visible { display: flex; }
@keyframes shakeX {
  0%,100%{transform:translateX(0)}
  10%,50%,90%{transform:translateX(-6px)}
  30%,70%{transform:translateX(6px)}
}
.login-error svg { width: 16px; height: 16px; flex-shrink: 0; }

/* Remember + Forgot row */
.login-meta {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 22px;
}
.remember-wrap {
  display: flex; align-items: center; gap: 8px;
  cursor: pointer; user-select: none;
}
.remember-wrap input[type="checkbox"] {
  width: 16px; height: 16px;
  accent-color: var(--blue);
  cursor: pointer;
}
.remember-wrap span { font-size: 13px; color: var(--text-muted); }
.forgot-link {
  font-size: 13px; color: var(--blue); font-weight: 500;
  background: none; border: none; cursor: pointer;
  font-family: inherit; padding: 0;
  transition: opacity .15s;
}
.forgot-link:hover { opacity: .75; }
.forgot-link:focus { outline: 2px solid var(--blue); outline-offset: 2px; border-radius: 3px; }

/* Login Button */
.login-btn {
  width: 100%;
  padding: 13px;
  background: var(--blue);
  color: #fff;
  border: none;
  border-radius: var(--radius-sm);
  font-family: 'DM Sans', sans-serif;
  font-size: 15px; font-weight: 600;
  cursor: pointer;
  transition: all .15s;
  display: flex; align-items: center; justify-content: center; gap: 8px;
  box-shadow: 0 2px 8px rgba(29,78,216,.3);
  letter-spacing: -.01em;
}
.login-btn:hover:not(:disabled) {
  background: #1e40af;
  transform: translateY(-1px);
  box-shadow: 0 4px 16px rgba(29,78,216,.35);
}
.login-btn:active:not(:disabled) { transform: translateY(0); }
.login-btn:disabled {
  background: #93c5fd;
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
}
.login-btn:focus { outline: 2px solid var(--blue); outline-offset: 3px; }
.login-btn .btn-spinner {
  width: 17px; height: 17px;
  border: 2px solid rgba(255,255,255,.4);
  border-top-color: #fff;
  border-radius: 50%;
  animation: spin .6s linear infinite;
  display: none;
}
.login-btn.loading .btn-spinner { display: block; }
.login-btn.loading .btn-text { display: none; }

/* Demo hint */
.demo-hint {
  margin-top: 20px;
  padding: 12px 14px;
  background: var(--blue-light);
  border: 1px solid var(--blue-mid);
  border-radius: var(--radius-xs);
  font-size: 12.5px;
  color: var(--blue);
  line-height: 1.6;
}
.demo-hint strong { font-weight: 600; }
.demo-hint-row {
  display: flex; gap: 4px; align-items: baseline; flex-wrap: wrap;
}
.demo-hint-fill {
  background: none; border: none; cursor: pointer;
  font-family: 'DM Mono', monospace; font-size: 12px;
  color: var(--blue); text-decoration: underline; padding: 0;
  font-weight: 500;
}
.demo-hint-fill:hover { opacity: .75; }

/* ─── VIEWS ─── */
.view { display: none; }
.view.active { display: block; }

/* ─── SKELETON LOADER ─── */
.skeleton-overlay {
  position: fixed; inset: 0;
  background: rgba(241,245,249,.85);
  z-index: 1500;
  display: none; align-items: center; justify-content: center;
  flex-direction: column; gap: 16px;
  backdrop-filter: blur(2px);
}
.skeleton-overlay.active { display: flex; }
.skeleton-spinner {
  width: 36px; height: 36px;
  border: 3px solid var(--border);
  border-top-color: var(--blue);
  border-radius: 50%;
  animation: spin .7s linear infinite;
}
.skeleton-text { font-size: 14px; color: var(--text-muted); font-weight: 500; }
@keyframes spin { to { transform: rotate(360deg); } }

/* ─── TOAST ─── */
.toast {
  position: fixed; bottom: 24px; left: 50%;
  transform: translateX(-50%) translateY(90px);
  background: #0F172A; color: #fff;
  padding: 12px 20px; border-radius: var(--radius-sm);
  font-size: 14px; font-weight: 500;
  display: flex; align-items: center; gap: 8px;
  z-index: 9999;
  transition: transform .35s cubic-bezier(.34,1.56,.64,1);
  white-space: nowrap;
  box-shadow: 0 8px 32px rgba(0,0,0,.2);
}
.toast.show { transform: translateX(-50%) translateY(0); }
.toast.error { background: var(--danger); }
.toast.warning { background: var(--warning); }
.toast-icon {
  width: 18px; height: 18px; background: var(--success);
  border-radius: 50%; display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.toast.error .toast-icon, .toast.warning .toast-icon { background: rgba(255,255,255,.25); }

/* ─── MODAL ─── */
.modal-backdrop {
  position: fixed; inset: 0;
  background: rgba(15,23,42,.45);
  z-index: 3000; display: none; align-items: center; justify-content: center;
  padding: 24px; backdrop-filter: blur(3px);
  opacity: 0; transition: opacity .2s ease-out;
  pointer-events: none;
  overflow-y: auto;
}
.modal-backdrop.active { 
  display: flex; 
  opacity: 1;
  pointer-events: auto;
}
.modal {
  background: var(--surface); border-radius: var(--radius);
  box-shadow: var(--shadow-lg); width: 100%; max-width: 540px;
  animation: modalIn .2s cubic-bezier(.34,1.56,.64,1);
  position: relative; overflow: visible;
  pointer-events: auto;
  max-height: 90vh;
  display: flex;
  flex-direction: column;
}
@keyframes modalIn { 
  from { opacity:0; transform: scale(.92) translateY(12px); }
  to { opacity: 1; transform: scale(1) translateY(0); }
}
.modal-header { 
  padding: 24px 24px 0; 
  display: flex; 
  align-items: flex-start; 
  justify-content: space-between; 
  gap: 16px;
  flex-shrink: 0;
  position: sticky;
  top: 0;
  background: var(--surface);
}
.modal-title { font-size: 17px; font-weight: 600; }
.modal-subtitle { font-size: 13px; color: var(--text-muted); margin-top: 2px; }
.modal-close {
  width: 30px; height: 30px; border-radius: 6px; border: none;
  background: var(--bg); cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  color: var(--text-muted); flex-shrink: 0; transition: all .15s;
  position: sticky;
  top: 24px;
  right: 24px;
}
.modal-close:hover { background: var(--border); color: var(--text); }
.modal-close:focus { outline: 2px solid var(--blue); outline-offset: 2px; }
.modal-body { 
  padding: 20px 24px 24px;
  overflow-y: auto;
  flex: 1;
  min-height: 0;
}
.modal-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 20px; }
.modal-field label { font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: .06em; color: var(--text-faint); display: block; margin-bottom: 4px; }
.modal-field span { font-size: 14px; font-weight: 500; color: var(--text); }
.modal-field .mono { font-family: 'DM Mono', monospace; font-size: 13px; }
.modal-accent { height: 4px; background: linear-gradient(90deg, var(--blue) 0%, #7c3aed 100%); border-radius: var(--radius) var(--radius) 0 0; }

/* ─── EMPTY STATE ─── */
.empty-state { padding: 60px 24px; text-align: center; display: none; }
.empty-state.visible { display: block; }
.empty-icon { width: 52px; height: 52px; background: var(--bg); border-radius: 14px; margin: 0 auto 14px; display: flex; align-items: center; justify-content: center; color: var(--text-faint); }
.empty-state h4 { font-size: 15px; font-weight: 600; color: var(--text); margin-bottom: 4px; }
.empty-state p { font-size: 13px; color: var(--text-muted); }

/* ===================================
   FORGOT PASSWORD MODAL
=================================== */
.forgot-modal-backdrop {
  position: fixed; inset: 0;
  background: rgba(15,23,42,.5);
  z-index: 4000; display: none; align-items: center; justify-content: center;
  padding: 24px; backdrop-filter: blur(4px);
}
.forgot-modal-backdrop.active { display: flex; }
.forgot-modal {
  background: var(--surface); border-radius: var(--radius);
  box-shadow: var(--shadow-xl); width: 100%; max-width: 420px;
  padding: 36px;
  animation: modalIn .2s cubic-bezier(.34,1.56,.64,1);
  text-align: center;
}
.forgot-modal-icon {
  width: 56px; height: 56px;
  background: var(--blue-light); border-radius: 50%;
  margin: 0 auto 18px;
  display: flex; align-items: center; justify-content: center;
  color: var(--blue);
}
.forgot-modal h3 { font-size: 18px; font-weight: 600; margin-bottom: 8px; }
.forgot-modal p { font-size: 13px; color: var(--text-muted); margin-bottom: 24px; line-height: 1.6; }
.forgot-modal-actions { display: flex; gap: 10px; }
.forgot-modal-cancel {
  flex: 1; padding: 11px; border: 1.5px solid var(--border);
  border-radius: var(--radius-sm); background: var(--surface);
  font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 500;
  cursor: pointer; transition: background .15s; color: var(--text);
}
.forgot-modal-cancel:hover { background: var(--bg); }
.forgot-modal-send {
  flex: 1; padding: 11px; background: var(--blue); color: #fff;
  border: none; border-radius: var(--radius-sm);
  font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 500;
  cursor: pointer; transition: background .15s;
}
.forgot-modal-send:hover { background: #1e40af; }

/* ===================================
   LOGOUT BUTTON STYLES
=================================== */
.logout-btn {
  display: inline-flex; align-items: center; gap: 7px;
  padding: 7px 14px; border-radius: var(--radius-xs);
  font-family: 'DM Sans', sans-serif; font-size: 13px; font-weight: 500;
  border: 1.5px solid var(--border-strong);
  background: var(--surface); color: var(--text-muted);
  cursor: pointer; transition: all .15s;
}
.logout-btn:hover { background: var(--danger-bg); border-color: var(--danger); color: var(--danger); }
.logout-btn:focus { outline: 2px solid var(--blue); outline-offset: 2px; }
.logout-btn svg { width: 14px; height: 14px; }

.sidebar-logout {
  display: flex; align-items: center; gap: 8px;
  padding: 9px 12px; border-radius: var(--radius-xs);
  font-size: 13px; font-weight: 500;
  color: var(--text-muted); cursor: pointer; transition: all .15s;
  background: none; border: none; font-family: inherit;
  width: 100%; text-align: left; margin-top: 8px;
}
.sidebar-logout:hover { background: var(--danger-bg); color: var(--danger); }
.sidebar-logout:focus { outline: 2px solid var(--blue); outline-offset: 2px; }
.sidebar-logout svg { width: 15px; height: 15px; flex-shrink: 0; }

/* ===================================
   SESSION USER BADGE (Topnav)
=================================== */
.nav-user-wrap {
  display: flex; align-items: center; gap: 10px;
}
.nav-user-info {
  text-align: right;
}
.nav-user-name { font-size: 13px; font-weight: 600; color: var(--text); line-height: 1.2; }
.nav-user-role { font-size: 11px; color: var(--text-muted); }

/* ===================================
   LECTURER VIEW
=================================== */
.lecturer-wrap { min-height: 100vh; display: flex; flex-direction: column; }
.topnav {
  background: var(--surface); border-bottom: 1px solid var(--border);
  padding: 0 24px; display: flex; align-items: center;
  height: 60px; position: sticky; top: 0; z-index: 100;
}
.nav-logo {
  display: flex; align-items: center; gap: 8px;
  font-weight: 600; font-size: 16px; color: var(--text);
  margin-right: 40px; text-decoration: none;
}
.nav-logo-icon {
  width: 30px; height: 30px; background: var(--blue); border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
}
.nav-logo-icon svg { width: 16px; height: 16px; }
.nav-links { display: flex; gap: 4px; flex: 1; }
.nav-link {
  padding: 6px 12px; border-radius: var(--radius-xs);
  font-size: 14px; font-weight: 500; color: var(--text-muted);
  cursor: pointer; transition: all .15s; text-decoration: none;
  border: none; background: transparent; font-family: inherit;
}
.nav-link:hover { background: var(--bg); color: var(--text); }
.nav-link.active { background: var(--blue-light); color: var(--blue); }
.nav-link:focus { outline: 2px solid var(--blue); outline-offset: 2px; }
.nav-avatar {
  width: 34px; height: 34px; border-radius: 50%;
  background: linear-gradient(135deg, var(--blue), #7c3aed);
  color: #fff; font-size: 12px; font-weight: 600;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}

.lec-page { display: none; }
.lec-page.active { display: block; }
.lec-main { max-width: 900px; margin: 0 auto; padding: 32px 24px; width: 100%; }
.page-header { margin-bottom: 24px; }
.page-header h1 { font-size: 22px; font-weight: 600; }
.page-header p { font-size: 14px; color: var(--text-muted); margin-top: 2px; }

.status-card {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 28px; margin-bottom: 20px;
  box-shadow: var(--shadow); position: relative; overflow: hidden;
}
.status-card::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0;
  height: 3px; background: var(--blue);
  border-radius: var(--radius) var(--radius) 0 0; transition: background .3s;
}
.status-card.checked-in::before { background: var(--success); }
.status-card.checked-out::before { background: var(--text-muted); }
.status-row {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 20px; flex-wrap: wrap; gap: 12px;
}
.status-info h2 {
  font-size: 13px; font-weight: 500; color: var(--text-muted);
  text-transform: uppercase; letter-spacing: .06em; margin-bottom: 6px;
}
.status-badge {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 6px 14px; border-radius: 20px;
  font-size: 14px; font-weight: 500; transition: all .2s;
}
.status-badge.out { background: var(--bg); color: var(--text-muted); border: 1px solid var(--border); }
.status-badge.in { background: var(--success-bg); color: var(--success); border: 1px solid var(--success-border); }
.status-badge.done { background: var(--bg); color: var(--text-muted); border: 1px solid var(--border); }
.status-dot { width: 8px; height: 8px; border-radius: 50%; background: currentColor; }
.status-dot.pulse { animation: pulse 2s infinite; }
@keyframes pulse { 0%,100%{opacity:1;transform:scale(1)} 50%{opacity:.6;transform:scale(.85)} }
.status-meta { font-size: 13px; color: var(--text-muted); margin-top: 4px; }
.status-meta span { font-family: 'DM Mono', monospace; color: var(--text); font-size: 13px; }

.checkin-btn {
  display: inline-flex; align-items: center; gap: 8px;
  padding: 13px 28px; border-radius: var(--radius-sm);
  font-family: 'DM Sans', sans-serif; font-size: 15px; font-weight: 600;
  border: none; cursor: pointer; transition: all .15s; letter-spacing: -.01em;
}
.checkin-btn:focus { outline: 2px solid var(--blue); outline-offset: 3px; }
.checkin-btn.primary { background: var(--blue); color: #fff; box-shadow: 0 2px 8px rgba(29,78,216,.25); }
.checkin-btn.primary:hover { background: #1e40af; transform: translateY(-1px); box-shadow: 0 4px 16px rgba(29,78,216,.3); }
.checkin-btn.primary:disabled { background: #93c5fd; cursor: not-allowed; transform: none; box-shadow: none; }
.checkin-btn.checkout { background: var(--surface); color: var(--danger); border: 1.5px solid var(--danger); }
.checkin-btn.checkout:hover { background: var(--danger-bg); }
.checkin-btn.checkout:disabled { opacity: .45; cursor: not-allowed; }
.checkin-btn:active:not(:disabled) { transform: translateY(0); }

.details-row { display: flex; gap: 24px; flex-wrap: wrap; padding-top: 20px; border-top: 1px solid var(--border); }
.detail-item label { font-size: 12px; color: var(--text-faint); text-transform: uppercase; letter-spacing: .06em; display: block; margin-bottom: 2px; }
.detail-item span { font-size: 14px; font-weight: 500; color: var(--text); }

.live-clock {
  font-family: 'DM Mono', monospace; font-size: 13px;
  color: var(--blue); background: var(--blue-light);
  padding: 3px 10px; border-radius: 6px; margin-left: 12px;
}

.section-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow); overflow: hidden; }
.section-header { padding: 20px 24px; border-bottom: 1.5px solid var(--border); display: flex; align-items: center; justify-content: space-between; background: linear-gradient(180deg, var(--bg) 0%, transparent 100%); }
.section-header h3 { font-size: 16px; font-weight: 700; color: var(--text); }
.section-header .link-btn { font-size: 13px; color: var(--blue); cursor: pointer; font-weight: 600; border: none; background: none; font-family: inherit; padding: 6px 0; transition: all .2s; }
.section-header .link-btn:hover { opacity: .75; }
.section-header .link-btn:focus { outline: 2px solid var(--blue); outline-offset: 2px; border-radius: 4px; }

table { width: 100%; border-collapse: collapse; min-width: 900px; }
th { font-size: 12px; font-weight: 500; text-transform: uppercase; letter-spacing: .06em; color: var(--text-faint); padding: 10px 24px; text-align: left; background: var(--bg); border-bottom: 1px solid var(--border); user-select: none; }
th.sortable { cursor: pointer; }
th.sortable:hover { color: var(--text-muted); }
th.sort-asc .sort-indicator::after { content: ' ↑'; }
th.sort-desc .sort-indicator::after { content: ' ↓'; }
td { padding: 14px 24px; font-size: 14px; color: var(--text); border-bottom: 1px solid var(--border); }
tr:last-child td { border-bottom: none; }
tr:hover td { background: var(--bg); cursor: pointer; }
.mono { font-family: 'DM Mono', monospace; font-size: 13px; }
.chip { display: inline-flex; align-items: center; gap: 5px; padding: 3px 10px; border-radius: 20px; font-size: 12px; font-weight: 500; }
.chip.present { background: var(--success-bg); color: var(--success); }
.chip.late { background: var(--warning-bg); color: var(--warning); }
.chip.absent { background: var(--danger-bg); color: var(--danger); }

.profile-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 32px; box-shadow: var(--shadow); margin-bottom: 20px; }
.profile-header { display: flex; align-items: center; gap: 20px; padding-bottom: 24px; margin-bottom: 24px; border-bottom: 1px solid var(--border); }
.profile-avatar { width: 72px; height: 72px; border-radius: 50%; background: linear-gradient(135deg, var(--blue) 0%, #7c3aed 100%); color: #fff; font-size: 26px; font-weight: 600; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
.profile-name { font-size: 20px; font-weight: 600; }
.profile-title { font-size: 14px; color: var(--text-muted); margin-top: 2px; }
.profile-badge { display: inline-flex; align-items: center; gap: 5px; background: var(--success-bg); color: var(--success); border: 1px solid var(--success-border); font-size: 12px; font-weight: 500; padding: 3px 10px; border-radius: 20px; margin-top: 8px; }
.profile-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.profile-field label { font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: .06em; color: var(--text-faint); display: block; margin-bottom: 4px; }
.profile-field span { font-size: 14px; font-weight: 500; }
.profile-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 14px; margin-top: 20px; }
.stat-box { background: var(--bg); border-radius: var(--radius-sm); padding: 16px; text-align: center; }
.stat-box .val { font-size: 26px; font-weight: 600; color: var(--blue); letter-spacing: -.02em; }
.stat-box .lbl { font-size: 12px; color: var(--text-muted); margin-top: 2px; }

/* ===================================
   ADMIN VIEW
=================================== */
.admin-wrap { display: flex; min-height: 100vh; }
.sidebar {
  width: 230px; background: var(--surface); border-right: 1px solid var(--border);
  display: flex; flex-direction: column;
  position: fixed; top: 0; left: 0; bottom: 0;
  z-index: 100; padding: 20px 12px;
}
.sidebar-logo { display: flex; align-items: center; gap: 8px; font-weight: 600; font-size: 16px; color: var(--text); padding: 8px 12px; margin-bottom: 24px; }
.sidebar-section { font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: .08em; color: var(--text-faint); padding: 0 12px; margin-bottom: 6px; margin-top: 16px; }
.sidebar-item {
  display: flex; align-items: center; gap: 10px;
  padding: 9px 12px; border-radius: var(--radius-xs);
  font-size: 14px; font-weight: 500; color: var(--text-muted);
  cursor: pointer; transition: all .15s; margin-bottom: 2px;
  border: none; background: transparent; font-family: inherit;
  width: 100%; text-align: left;
}
.sidebar-item:hover { background: var(--bg); color: var(--text); }
.sidebar-item.active { background: var(--blue-light); color: var(--blue); }
.sidebar-item:focus { outline: 2px solid var(--blue); outline-offset: 2px; }
.sidebar-item svg { width: 16px; height: 16px; flex-shrink: 0; }
.sidebar-badge { margin-left: auto; background: var(--danger); color: #fff; font-size: 11px; font-weight: 600; padding: 1px 7px; border-radius: 10px; }
.sidebar-user { margin-top: auto; padding: 12px; border-top: 1px solid var(--border); display: flex; align-items: center; gap: 10px; }
.sidebar-user-avatar { width: 34px; height: 34px; border-radius: 50%; background: linear-gradient(135deg, var(--blue), #7c3aed); color: #fff; font-size: 12px; font-weight: 600; display: flex; align-items: center; justify-content: center; }
.sidebar-user-info { flex: 1; min-width: 0; }
.sidebar-user-info p { font-size: 13px; font-weight: 600; color: var(--text); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.sidebar-user-info span { font-size: 12px; color: var(--text-muted); }

.admin-main { margin-left: 230px; flex: 1; padding: 32px; min-height: 100vh; }
.admin-section { display: none; }
.admin-section.active { display: block; }
.admin-header { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 28px; flex-wrap: wrap; gap: 16px; }
.admin-header h1 { font-size: 24px; font-weight: 700; color: var(--text); letter-spacing: -.01em; }
.admin-header p { font-size: 14px; color: var(--text-muted); margin-top: 4px; font-weight: 500; }
.export-btn { display: inline-flex; align-items: center; justify-content: center; gap: 8px; padding: 10px 18px; border-radius: 8px; font-family: 'DM Sans', sans-serif; font-size: 13px; font-weight: 600; border: 1.5px solid var(--border); background: var(--surface); color: var(--text); cursor: pointer; transition: all .2s cubic-bezier(.4,0,.2,1); }
.export-btn:hover { background: var(--bg); border-color: var(--blue-light); transform: translateY(-1px); }
.export-btn:focus { outline: 2px solid var(--blue); outline-offset: 2px; }
.export-btn.primary-btn { background: var(--blue); color: #fff; border-color: var(--blue); box-shadow: 0 4px 12px rgba(29,78,216,.2); }
.export-btn.primary-btn:hover { background: #1e40af; box-shadow: 0 6px 16px rgba(29,78,216,.3); }
.flex { display: flex; }
.items-center { align-items: center; }
.gap-8 { gap: 8px; }

.filters-bar { background: var(--surface); border: 1.5px solid var(--border); border-radius: 12px; padding: 18px 22px; margin-bottom: 24px; display: flex; gap: 14px; align-items: flex-end; flex-wrap: wrap; box-shadow: 0 2px 8px rgba(0,0,0,.04); }
.filter-group { display: flex; flex-direction: column; gap: 6px; }
.filter-group label { font-size: 11px; font-weight: 600; color: var(--text-muted); text-transform: uppercase; letter-spacing: .05em; }
.filter-input { padding: 10px 12px; border: 1.5px solid var(--border); border-radius: 8px; font-family: 'DM Sans', sans-serif; font-size: 13px; color: var(--text); background: var(--bg); outline: none; transition: all .2s; min-width: 140px; }
.filter-input:focus { border-color: var(--blue); background: #fff; box-shadow: 0 0 0 3px rgba(29,78,216,.1); }
.filter-input::placeholder { color: var(--text-faint); }
.filter-search { position: relative; flex: 1; min-width: 200px; }
.filter-search input { padding-left: 36px; width: 100%; }
.filter-search svg { position: absolute; left: 11px; top: 50%; transform: translateY(-50%); width: 15px; height: 15px; color: var(--text-faint); pointer-events: none; }
.status-select { padding: 10px 12px; }

.kpi-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 16px; margin-bottom: 24px; }
.kpi-card { background: var(--surface); border: 1.5px solid var(--border); border-radius: 12px; padding: 24px; box-shadow: 0 2px 8px rgba(0,0,0,.04); position: relative; overflow: hidden; transition: all .25s cubic-bezier(.4,0,.2,1); }
.kpi-card:hover { transform: translateY(-6px) scale(1.02); box-shadow: 0 12px 28px rgba(0,0,0,.12); border-color: var(--blue); }
.kpi-card::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; background: linear-gradient(90deg, currentColor, transparent); opacity: 0.4; }
.kpi-card::after { content: ''; position: absolute; bottom: 0; left: 0; right: 0; top: 0; background: radial-gradient(circle at bottom right, currentColor 0%, transparent 80%); opacity: 0.03; pointer-events: none; }
.kpi-card.k-present::before { color: var(--success); }
.kpi-card.k-late::before { color: var(--warning); }
.kpi-card.k-absent::before { color: var(--danger); }
.kpi-card.k-total::before { color: var(--blue); }
.kpi-label { font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: .07em; color: var(--text-faint); margin-bottom: 10px; }
.kpi-value { font-size: 36px; font-weight: 700; letter-spacing: -.03em; line-height: 1; margin-bottom: 8px; transition: all .3s cubic-bezier(.4,0,.2,1); font-variant-numeric: tabular-nums; }
.kpi-card.k-present .kpi-value { color: var(--success); }
.kpi-card.k-late .kpi-value { color: var(--warning); }
.kpi-card.k-absent .kpi-value { color: var(--danger); }
.kpi-card.k-total .kpi-value { color: var(--blue); }
.kpi-sub { font-size: 12px; color: var(--text-muted); font-weight: 500; }

.table-card { background: var(--surface); border: 1.5px solid var(--border); border-radius: 12px; box-shadow: 0 2px 8px rgba(0,0,0,.04); overflow-x: auto; overflow-y: hidden; }
.table-actions { display: flex; gap: 8px; align-items: center; }
.sort-indicator { color: var(--text-faint); font-size: 11px; margin-left: 4px; }

.pagination { display: flex; align-items: center; justify-content: space-between; padding: 14px 24px; border-top: 1px solid var(--border); flex-wrap: wrap; gap: 10px; }
.pagination-info { font-size: 13px; color: var(--text-muted); }
.pagination-controls { display: flex; gap: 4px; }
.page-btn { width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; border-radius: var(--radius-xs); border: 1px solid var(--border); background: var(--surface); font-size: 13px; font-weight: 500; color: var(--text-muted); cursor: pointer; font-family: 'DM Mono', monospace; transition: all .15s; }
.page-btn.active { background: var(--blue); color: #fff; border-color: var(--blue); }
.page-btn:hover:not(.active):not(:disabled) { background: var(--bg); }
.page-btn:disabled { opacity: .4; cursor: not-allowed; }
.page-btn:focus { outline: 2px solid var(--blue); outline-offset: 2px; }

.dash-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(380px, 1fr)); gap: 18px; margin-bottom: 24px; }
.dash-card { background: var(--surface); border: 1.5px solid var(--border); border-radius: 12px; padding: 24px; box-shadow: 0 2px 8px rgba(0,0,0,.04); transition: all .2s ease; }
.dash-card:hover { border-color: var(--blue-light); box-shadow: 0 8px 20px rgba(29,78,216,.08); }
.dash-card h3 { font-size: 15px; font-weight: 700; color: var(--text); margin-bottom: 20px; }
.bar-chart { display: flex; flex-direction: column; gap: 10px; }
.bar-row { display: flex; align-items: center; gap: 10px; }
.bar-label { font-size: 12px; color: var(--text-muted); width: 90px; flex-shrink: 0; text-overflow: ellipsis; overflow: hidden; white-space: nowrap; }
.bar-track { flex: 1; background: var(--bg); border-radius: 4px; height: 8px; overflow: hidden; }
.bar-fill { height: 100%; border-radius: 4px; transition: width .5s cubic-bezier(.4,0,.2,1); }
.bar-val { font-size: 12px; font-family: 'DM Mono', monospace; color: var(--text-muted); width: 28px; text-align: right; }
.donut-wrap { display: flex; align-items: center; gap: 20px; }
.donut-legend { display: flex; flex-direction: column; gap: 10px; }
.donut-item { display: flex; align-items: center; gap: 8px; font-size: 13px; }
.donut-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; }

.lecturer-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(340px, 1fr)); gap: 18px; }
.lecturer-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); box-shadow: var(--shadow); cursor: pointer; transition: all .2s cubic-bezier(.4,0,.2,1); overflow: hidden; position: relative; }
.lecturer-card:hover { transform: translateY(-4px) scale(1.01); box-shadow: 0 12px 32px rgba(29,78,216,.15); border-color: var(--blue); }
.lec-card-accent { position: absolute; top: 0; left: 0; right: 0; height: 3px; }
.lec-card-content { padding: 24px; position: relative; z-index: 1; }
.lec-card-top { display: flex; align-items: flex-start; gap: 14px; margin-bottom: 20px; }
.lec-avatar { width: 52px; height: 52px; border-radius: 12px; font-size: 18px; font-weight: 700; color: #fff; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
.lec-info-wrap { flex: 1; }
.lec-name-group { display: flex; align-items: center; gap: 8px; margin-bottom: 4px; }
.lec-name { font-size: 16px; font-weight: 700; color: var(--text); margin: 0; }
.lec-dept-badge { display: inline-flex; align-items: center; justify-content: center; padding: 3px 8px; border-radius: 6px; font-size: 11px; font-weight: 600; text-transform: uppercase; letter-spacing: .04em; }
.lec-id { font-size: 12px; color: var(--text-muted); font-family: 'DM Mono', monospace; margin: 0; }
.lec-status-badge { display: inline-flex; align-items: center; justify-content: center; padding: 5px 10px; border-radius: 6px; font-size: 12px; font-weight: 600; white-space: nowrap; }
.lec-status-badge.present { background: var(--success-bg); color: var(--success); }
.lec-status-badge.late { background: var(--warning-bg); color: var(--warning); }
.lec-status-badge.absent { background: var(--danger-bg); color: var(--danger); }
.lec-stats-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 12px; }
.lec-stat-item { background: var(--bg); padding: 12px; border-radius: 8px; border: 1px solid var(--border); }
.lec-stat-number { font-size: 20px; font-weight: 700; color: var(--text); letter-spacing: -.02em; }
.lec-stat-label { font-size: 11px; color: var(--text-faint); text-transform: uppercase; letter-spacing: .05em; margin-top: 2px; margin-bottom: 6px; font-weight: 500; }
.lec-stat-bar { width: 100%; height: 4px; background: var(--border); border-radius: 2px; overflow: hidden; }
.lec-stat-bar-fill { height: 100%; border-radius: 2px; transition: width .4s cubic-bezier(.4,0,.2,1); }

.settings-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
.settings-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--radius); padding: 24px; box-shadow: var(--shadow); }
.settings-card h3 { font-size: 15px; font-weight: 600; margin-bottom: 16px; }
.settings-field { margin-bottom: 14px; }
.settings-field label { font-size: 13px; font-weight: 500; color: var(--text-muted); display: block; margin-bottom: 6px; }
.settings-input { width: 100%; padding: 9px 12px; border: 1px solid var(--border); border-radius: var(--radius-xs); font-family: 'DM Sans', sans-serif; font-size: 14px; color: var(--text); background: var(--bg); outline: none; transition: border-color .15s; }
.settings-input:focus { border-color: var(--blue); background: #fff; }
.toggle-row { display: flex; align-items: center; justify-content: space-between; padding: 12px 0; border-bottom: 1px solid var(--border); }
.toggle-row:last-child { border-bottom: none; }
.toggle-info p { font-size: 14px; font-weight: 500; }
.toggle-info span { font-size: 12px; color: var(--text-muted); }
.toggle { width: 42px; height: 24px; background: var(--border-strong); border-radius: 12px; position: relative; cursor: pointer; transition: background .2s; border: none; flex-shrink: 0; }
.toggle.on { background: var(--success); }
.toggle::after { content: ''; position: absolute; width: 18px; height: 18px; border-radius: 50%; background: #fff; top: 3px; left: 3px; transition: transform .2s; box-shadow: 0 1px 4px rgba(0,0,0,.15); }
.toggle.on::after { transform: translateX(18px); }
.save-btn { display: inline-flex; align-items: center; gap: 7px; padding: 10px 20px; border-radius: var(--radius-sm); background: var(--blue); color: #fff; border: none; font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 500; cursor: pointer; transition: all .15s; margin-top: 16px; }
.save-btn:hover { background: #1e40af; }
.save-btn:focus { outline: 2px solid var(--blue); outline-offset: 2px; }

/* Responsive tweaks */
@media (max-width: 1280px) {
  .lecturer-grid { grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); }
  .kpi-grid { grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); }
}
@media (max-width: 1024px) {
  .admin-main { padding: 24px; }
  .sidebar { width: 200px; }
  .admin-main { margin-left: 200px; }
  .lecturer-grid { grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 16px; }
  .kpi-grid { grid-template-columns: repeat(auto-fit, minmax(160px, 1fr)); gap: 14px; }
  .dash-grid { grid-template-columns: 1fr; }
  .admin-quick-stats { grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); }
}
@media (max-width: 768px) {
  .admin-main { margin-left: 0; padding: 20px; }
  .admin-wrap { flex-direction: column; }
  .sidebar { width: 100%; position: static; height: auto; }
  .lecturer-grid { grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 14px; }
  .lec-stats-grid { grid-template-columns: 1fr 1fr; }
  .kpi-grid { grid-template-columns: repeat(2, 1fr); gap: 12px; }
  .kpi-value { font-size: 28px; }
  .kpi-label { font-size: 10px; }
  .admin-header { flex-direction: column; }
  .admin-header h1 { font-size: 20px; }
  .filters-bar { flex-direction: column; padding: 16px; gap: 12px; }
  .filter-group { width: 100%; }
  .filter-input { width: 100%; min-width: unset; }
  .filter-search { min-width: unset; }
  .admin-quick-stats { grid-template-columns: 1fr; gap: 12px; }
  .aqs-card { padding: 16px; gap: 12px; }
  .aqs-val { font-size: 24px; }
}
@media (max-width: 640px) {
  .lecturer-grid { grid-template-columns: 1fr; }
  .lec-stats-grid { grid-template-columns: 1fr; }
  .kpi-grid { grid-template-columns: 1fr; }
  .lec-card-top { flex-wrap: wrap; }
  .lec-status-badge { width: 100%; justify-content: center; margin-top: 8px; }
  .kpi-value { font-size: 24px; }
  .section-header { flex-direction: column; align-items: flex-start; gap: 12px; }
}
@media (max-width: 780px) {
  .login-brand { display: none; }
  .login-form-panel { padding: 36px 28px; }
  .login-split { max-width: 440px; }
}
@media (max-width: 500px) {
  .login-form-panel { padding: 28px 20px; }
}


/* ===================================================================
   GLOBAL ENHANCEMENTS - Dark mode, transitions, micro-interactions
=================================================================== */
:root {
  --purple: #7c3aed;
  --purple-light: #f5f3ff;
  --purple-mid: #ede9fe;
  --teal: #0891b2;
  --teal-light: #ecfeff;
  --teal-mid: #cffafe;
  --orange: #ea580c;
  --orange-light: #fff7ed;
  --sidebar-w: 240px;
}

/* Dark mode */
[data-theme="dark"] {
  --bg: #0f172a;
  --surface: #1e293b;
  --text: #f1f5f9;
  --text-muted: #94a3b8;
  --text-faint: #64748b;
  --border: #334155;
  --border-strong: #475569;
  --blue-light: #1e3a5f;
  --blue-mid: #1e3a5f;
}
[data-theme="dark"] .topnav,
[data-theme="dark"] .sidebar,
[data-theme="dark"] .section-card,
[data-theme="dark"] .status-card,
[data-theme="dark"] .profile-card,
[data-theme="dark"] .dash-card,
[data-theme="dark"] .kpi-card,
[data-theme="dark"] .table-card,
[data-theme="dark"] .filters-bar,
[data-theme="dark"] .settings-card,
[data-theme="dark"] .export-btn,
[data-theme="dark"] .page-btn,
[data-theme="dark"] .login-form-panel { background: var(--surface); border-color: var(--border); }
[data-theme="dark"] td, [data-theme="dark"] th { border-color: var(--border); }
[data-theme="dark"] th { background: #0f172a; }
[data-theme="dark"] tr:hover td { background: #0f172a; }
[data-theme="dark"] .filter-input,
[data-theme="dark"] .settings-input { background: #0f172a; color: var(--text); border-color: var(--border); }
[data-theme="dark"] .stat-box { background: #0f172a; }
[data-theme="dark"] .empty-icon { background: #0f172a; }

/* Smooth page transitions */
.view { animation: none; }
.view.active { animation: viewFadeIn .25s ease-out; }
@keyframes viewFadeIn { from { opacity: 0; transform: translateY(8px); } to { opacity: 1; transform: translateY(0); } }

/* ===================================================================
   LOGIN PAGE - Student role button & improved switcher
=================================================================== */
.role-btn.role-student {
  background: transparent;
  color: var(--text-muted);
}
.role-btn.role-student.active {
  background: var(--surface);
  color: var(--teal);
}
.demo-hint-row.student-row { margin-top: 2px; }

/* ===================================================================
   STUDENT VIEW
=================================================================== */
.student-wrap { min-height: 100vh; display: flex; flex-direction: column; background: var(--bg); }

/* Student topnav - teal accent */
.student-topnav {
  background: var(--surface);
  border-bottom: 1px solid var(--border);
  padding: 0 24px;
  display: flex; align-items: center;
  height: 60px; position: sticky; top: 0; z-index: 100;
  gap: 0;
}
.student-topnav .nav-logo-icon { background: var(--teal); }
.student-topnav .nav-link.active { background: var(--teal-light); color: var(--teal); }
.student-topnav .nav-link:focus { outline-color: var(--teal); }

/* Student pages */
.stu-page { display: none; }
.stu-page.active { display: block; }
.stu-main { max-width: 960px; margin: 0 auto; padding: 32px 24px; width: 100%; }

/* Welcome banner */
.stu-welcome {
  background: linear-gradient(135deg, var(--teal) 0%, #0369a1 100%);
  border-radius: var(--radius);
  padding: 28px 32px;
  color: #fff;
  margin-bottom: 24px;
  position: relative;
  overflow: hidden;
}
.stu-welcome::after {
  content: '';
  position: absolute; top: -40px; right: -40px;
  width: 200px; height: 200px;
  background: rgba(255,255,255,.07);
  border-radius: 50%;
}
.stu-welcome::before {
  content: '';
  position: absolute; bottom: -60px; right: 80px;
  width: 160px; height: 160px;
  background: rgba(255,255,255,.05);
  border-radius: 50%;
}
.stu-welcome-inner { position: relative; z-index: 1; }
.stu-welcome h1 { font-size: 22px; font-weight: 600; margin-bottom: 4px; }
.stu-welcome p { font-size: 14px; opacity: .8; }
.stu-welcome-stats {
  display: flex; gap: 24px; margin-top: 20px; flex-wrap: wrap;
}
.stu-welcome-stat {
  background: rgba(255,255,255,.15);
  border-radius: var(--radius-sm);
  padding: 10px 16px;
  backdrop-filter: blur(8px);
}
.stu-welcome-stat .sw-val { font-size: 22px; font-weight: 700; line-height: 1; }
.stu-welcome-stat .sw-lbl { font-size: 11px; opacity: .75; margin-top: 2px; text-transform: uppercase; letter-spacing: .05em; }

/* Today's schedule grid */
.schedule-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(290px, 1fr)); gap: 14px; margin-bottom: 24px; }
.schedule-slot {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 20px;
  box-shadow: var(--shadow);
  position: relative;
  overflow: hidden;
  transition: transform .15s, box-shadow .15s;
}
.schedule-slot-clickable {
  cursor: pointer;
}
.schedule-slot-clickable:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
  border-color: var(--blue);
}
.schedule-slot::before {
  content: '';
  position: absolute; left: 0; top: 0; bottom: 0;
  width: 4px;
  background: var(--teal);
  border-radius: var(--radius) 0 0 var(--radius);
}
.schedule-slot.status-present::before { background: var(--success); }
.schedule-slot.status-absent::before { background: var(--danger); }
.schedule-slot.status-pending::before { background: var(--warning); }
.slot-time { font-family: 'DM Mono', monospace; font-size: 12px; color: var(--text-faint); margin-bottom: 8px; }
.slot-subject { font-size: 15px; font-weight: 600; color: var(--text); margin-bottom: 4px; }
.slot-lecturer { font-size: 13px; color: var(--text-muted); margin-bottom: 12px; }
.slot-meta { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 8px; }
.slot-room { font-size: 12px; color: var(--text-faint); display: flex; align-items: center; gap: 4px; }
.slot-room svg { width: 13px; height: 13px; }

/* Status chips for schedule */
.chip.stu-present { background: var(--success-bg); color: var(--success); border: 1px solid var(--success-border); }
.chip.stu-absent  { background: var(--danger-bg); color: var(--danger); border: 1px solid var(--danger-border); }
.chip.stu-pending { background: var(--warning-bg); color: var(--warning); border: 1px solid var(--warning-border); }
.chip.stu-holiday { background: var(--purple-light); color: var(--purple); border: 1px solid var(--purple-mid); }

/* Attendance summary mini cards */
.stu-summary-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 24px; }
.stu-sum-card {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 16px 18px;
  box-shadow: var(--shadow); display: flex; align-items: center; gap: 14px;
  transition: transform .15s;
}
.stu-sum-card:hover { transform: translateY(-2px); }
.stu-sum-icon {
  width: 40px; height: 40px; border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
}
.stu-sum-icon svg { width: 19px; height: 19px; }
.stu-sum-icon.ic-present { background: var(--success-bg); color: var(--success); }
.stu-sum-icon.ic-absent  { background: var(--danger-bg); color: var(--danger); }
.stu-sum-icon.ic-late    { background: var(--warning-bg); color: var(--warning); }
.stu-sum-icon.ic-rate    { background: var(--teal-light); color: var(--teal); }
.stu-sum-val { font-size: 24px; font-weight: 700; letter-spacing: -.02em; line-height: 1; }
.stu-sum-lbl { font-size: 12px; color: var(--text-muted); margin-top: 2px; }

/* Student weekly schedule table */
.week-table-wrap { overflow-x: auto; }
.week-table { min-width: 700px; }
.week-table th { text-align: center; font-size: 11px; }
.week-table td { text-align: center; vertical-align: middle; }
.week-slot-cell {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  padding: 8px 10px;
  font-size: 12px;
  display: inline-block;
  min-width: 90px;
}
.week-slot-cell .ws-subj { font-weight: 600; color: var(--text); font-size: 12px; }
.week-slot-cell .ws-room { color: var(--text-faint); font-size: 11px; margin-top: 2px; }
.day-label { font-size: 11px; font-weight: 600; color: var(--text-muted); text-transform: uppercase; letter-spacing: .05em; }
.time-label { font-family: 'DM Mono', monospace; font-size: 12px; color: var(--text-faint); }

/* Student profile */
.stu-profile-card {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 32px;
  box-shadow: var(--shadow); margin-bottom: 20px;
}
.stu-profile-header {
  display: flex; align-items: center; gap: 24px;
  padding-bottom: 24px; margin-bottom: 24px;
  border-bottom: 1px solid var(--border);
}
.stu-avatar-wrap { position: relative; }
.stu-avatar {
  width: 80px; height: 80px; border-radius: 50%;
  background: linear-gradient(135deg, var(--teal) 0%, #0369a1 100%);
  color: #fff; font-size: 28px; font-weight: 700;
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0; cursor: pointer;
}
.stu-avatar-edit {
  position: absolute; bottom: 0; right: 0;
  width: 26px; height: 26px;
  background: var(--blue); color: #fff;
  border-radius: 50%; border: 2px solid var(--surface);
  display: flex; align-items: center; justify-content: center;
  cursor: pointer; transition: background .15s;
}
.stu-avatar-edit:hover { background: #1e40af; }
.stu-avatar-edit svg { width: 12px; height: 12px; }
.stu-profile-name { font-size: 22px; font-weight: 700; margin-bottom: 4px; }
.stu-profile-sub  { font-size: 14px; color: var(--text-muted); }
.stu-profile-tags { display: flex; gap: 8px; margin-top: 10px; flex-wrap: wrap; }
.stu-tag {
  display: inline-flex; align-items: center; gap: 5px;
  padding: 3px 10px; border-radius: 20px;
  font-size: 12px; font-weight: 500;
  background: var(--teal-light); color: var(--teal);
  border: 1px solid var(--teal-mid);
}
.stu-profile-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
.stu-profile-field label {
  font-size: 11px; font-weight: 600;
  text-transform: uppercase; letter-spacing: .06em;
  color: var(--text-faint); display: block; margin-bottom: 4px;
}
.stu-profile-field span { font-size: 14px; font-weight: 500; }
.stu-edit-btn {
  display: inline-flex; align-items: center; gap: 7px;
  padding: 10px 20px; border-radius: var(--radius-sm);
  background: var(--teal); color: #fff; border: none;
  font-family: 'DM Sans', sans-serif; font-size: 14px; font-weight: 500;
  cursor: pointer; transition: all .15s; margin-top: 20px;
}
.stu-edit-btn:hover { background: #0369a1; transform: translateY(-1px); }
.stu-edit-btn svg { width: 15px; height: 15px; }

/* ===================================================================
   LECTURER DASHBOARD - Enhanced UI
=================================================================== */

/* Improved status card with big visual indicator */
.status-card-v2 {
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 0;
  margin-bottom: 20px;
  box-shadow: var(--shadow);
  overflow: hidden;
}
.scv2-accent {
  height: 4px;
  background: linear-gradient(90deg, var(--blue), var(--purple));
  transition: background .4s;
}
.scv2-accent.checked-in-accent { background: linear-gradient(90deg, var(--success), #22c55e); }
.scv2-accent.checked-out-accent { background: linear-gradient(90deg, var(--text-faint), var(--border-strong)); }
.scv2-body { padding: 24px 28px; display: flex; align-items: center; gap: 24px; flex-wrap: wrap; }
.scv2-indicator {
  width: 80px; height: 80px; flex-shrink: 0;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  position: relative;
}
.scv2-indicator svg { width: 32px; height: 32px; }
.scv2-indicator.ind-out {
  background: var(--bg);
  color: var(--text-faint);
  box-shadow: 0 0 0 6px var(--border);
}
.scv2-indicator.ind-in {
  background: var(--success-bg);
  color: var(--success);
  box-shadow: 0 0 0 6px var(--success-border);
  animation: indicatorPulse 2.5s ease-in-out infinite;
}
.scv2-indicator.ind-done {
  background: var(--bg);
  color: var(--text-faint);
  box-shadow: 0 0 0 6px var(--border);
}
@keyframes indicatorPulse {
  0%,100% { box-shadow: 0 0 0 6px var(--success-border); }
  50% { box-shadow: 0 0 0 12px rgba(22,163,74,.1); }
}
.scv2-info { flex: 1; min-width: 200px; }
.scv2-status-label { font-size: 12px; font-weight: 500; color: var(--text-faint); text-transform: uppercase; letter-spacing: .06em; margin-bottom: 6px; }
.scv2-status-val { font-size: 22px; font-weight: 700; color: var(--text); margin-bottom: 4px; }
.scv2-meta { font-size: 13px; color: var(--text-muted); }
.scv2-meta span { font-family: 'DM Mono', monospace; color: var(--text); }
.scv2-actions { display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }
.scv2-footer {
  padding: 16px 28px;
  border-top: 1px solid var(--border);
  background: var(--bg);
  display: flex; gap: 28px; flex-wrap: wrap;
}
.scv2-detail { display: flex; flex-direction: column; gap: 2px; }
.scv2-detail .sd-label { font-size: 11px; color: var(--text-faint); text-transform: uppercase; letter-spacing: .06em; }
.scv2-detail .sd-val { font-size: 13px; font-weight: 500; }
.scv2-detail .sd-val.mono { font-family: 'DM Mono', monospace; font-size: 13px; }

/* Daily summary strip */
.daily-summary {
  display: flex; gap: 12px; margin-bottom: 20px; flex-wrap: wrap;
}
.ds-card {
  flex: 1; min-width: 140px;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius-sm); padding: 14px 16px;
  box-shadow: var(--shadow); display: flex; align-items: center; gap: 12px;
  transition: transform .15s;
}
.ds-card:hover { transform: translateY(-1px); }
.ds-icon { width: 34px; height: 34px; border-radius: 8px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
.ds-icon svg { width: 16px; height: 16px; }
.ds-icon.ic-classes { background: var(--blue-light); color: var(--blue); }
.ds-icon.ic-hours   { background: var(--success-bg); color: var(--success); }
.ds-icon.ic-rate    { background: var(--warning-bg); color: var(--warning); }
.ds-val { font-size: 20px; font-weight: 700; letter-spacing: -.02em; line-height: 1.1; }
.ds-lbl { font-size: 12px; color: var(--text-muted); }

/* ===================================================================
   ADMIN DASHBOARD - Enhanced UI
=================================================================== */

/* Improved KPI cards with trend indicators */
.kpi-card .kpi-trend {
  display: inline-flex; align-items: center; gap: 3px;
  font-size: 11px; font-weight: 600; margin-top: 4px;
  padding: 2px 7px; border-radius: 20px;
}
.kpi-trend.up   { background: var(--success-bg); color: var(--success); }
.kpi-trend.down { background: var(--danger-bg); color: var(--danger); }
.kpi-trend.flat { background: var(--bg); color: var(--text-faint); }

/* Activity feed */
.activity-feed { display: flex; flex-direction: column; gap: 0; }
.activity-item {
  display: flex; align-items: flex-start; gap: 12px;
  padding: 14px 24px; border-bottom: 1px solid var(--border);
  transition: background .15s;
}
.activity-item:last-child { border-bottom: none; }
.activity-item:hover { background: var(--bg); }
.activity-dot {
  width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0;
  margin-top: 6px;
}
.activity-dot.dot-present { background: var(--success); }
.activity-dot.dot-late    { background: var(--warning); }
.activity-dot.dot-absent  { background: var(--danger); }
.activity-text { font-size: 13px; color: var(--text); flex: 1; line-height: 1.5; }
.activity-text strong { font-weight: 600; }
.activity-time { font-size: 12px; color: var(--text-faint); font-family: 'DM Mono', monospace; flex-shrink: 0; }

/* Admin quick-stats row */
.admin-quick-stats {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 16px;
  margin-bottom: 24px;
}
.aqs-card {
  background: var(--surface); border: 1.5px solid var(--border);
  border-radius: 12px; padding: 22px;
  box-shadow: 0 2px 8px rgba(0,0,0,.04); display: flex; align-items: center; gap: 16px;
  transition: all .2s cubic-bezier(.4,0,.2,1);
}
.aqs-card:hover { transform: translateY(-3px); box-shadow: 0 10px 24px rgba(0,0,0,.08); border-color: var(--blue-light); }
.aqs-icon {
  width: 52px; height: 52px; border-radius: 10px;
  display: flex; align-items: center; justify-content: center; flex-shrink: 0;
  transition: all .2s;
}
.aqs-card:hover .aqs-icon { transform: scale(1.08); }
.aqs-icon svg { width: 24px; height: 24px; }
.aqs-icon.aqsi-total    { background: linear-gradient(135deg, var(--blue-light) 0%, var(--blue-light) 100%); color: var(--blue); }
.aqs-icon.aqsi-active   { background: linear-gradient(135deg, var(--success-bg) 0%, var(--success-bg) 100%); color: var(--success); }
.aqs-icon.aqsi-inactive { background: linear-gradient(135deg, var(--danger-bg) 0%, var(--danger-bg) 100%); color: var(--danger); }
.aqs-val { font-size: 28px; font-weight: 700; letter-spacing: -.02em; line-height: 1; font-variant-numeric: tabular-nums; }
.aqs-lbl { font-size: 13px; color: var(--text); margin-top: 3px; font-weight: 600; }
.aqs-sub { font-size: 11px; color: var(--text-muted); margin-top: 2px; }

/* Improved sidebar */
.sidebar { width: var(--sidebar-w); }
.admin-main { margin-left: var(--sidebar-w); }
.sidebar-version { padding: 8px 12px; font-size: 11px; color: var(--text-faint); margin-top: 4px; }

/* Sidebar hover effect with indicator bar */
.sidebar-item { position: relative; }
.sidebar-item.active::after {
  content: ''; position: absolute; right: 0; top: 4px; bottom: 4px;
  width: 3px; background: var(--blue); border-radius: 2px 0 0 2px;
}

/* ===================================================================
   SHARED - Improved table, buttons, cards, animations
=================================================================== */

/* Ripple effect on buttons */
.checkin-btn, .login-btn, .save-btn, .stu-edit-btn, .export-btn {
  position: relative; overflow: hidden;
}
.btn-ripple {
  position: absolute; border-radius: 50%;
  background: rgba(255,255,255,.35);
  transform: scale(0); animation: ripple .5s linear;
  pointer-events: none;
}
@keyframes ripple { to { transform: scale(4); opacity: 0; } }

/* Improved chip variants */
.chip { font-size: 11.5px; padding: 3px 9px; letter-spacing: .01em; }
.chip::before { content: ''; display: inline-block; width: 6px; height: 6px; border-radius: 50%; background: currentColor; margin-right: 5px; flex-shrink: 0; }

/* Better empty state */
.empty-state { animation: emptyFadeIn .3s ease-out; }
@keyframes emptyFadeIn { from { opacity: 0; transform: scale(.97); } to { opacity: 1; transform: scale(1); } }

/* Notification bell */
.nav-notif {
  width: 34px; height: 34px; border-radius: 50%;
  background: var(--bg); border: 1px solid var(--border);
  display: flex; align-items: center; justify-content: center;
  cursor: pointer; color: var(--text-muted);
  position: relative; transition: all .15s; flex-shrink: 0;
}
.nav-notif:hover { background: var(--blue-light); color: var(--blue); }
.nav-notif svg { width: 16px; height: 16px; }
.nav-notif-dot {
  position: absolute; top: 4px; right: 4px;
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--danger); border: 2px solid var(--surface);
}

/* Progress bar component */
.progress-bar { background: var(--border); border-radius: 4px; height: 6px; overflow: hidden; margin-top: 6px; }
.progress-fill { height: 100%; border-radius: 4px; transition: width .6s cubic-bezier(.4,0,.2,1); }
.progress-fill.pf-success { background: var(--success); }
.progress-fill.pf-warning { background: var(--warning); }
.progress-fill.pf-danger  { background: var(--danger); }
.progress-fill.pf-blue    { background: var(--blue); }
.progress-fill.pf-teal    { background: var(--teal); }

/* Table row hover transition */
tr td { transition: background .1s; }

/* Card skeleton loading */
.skeleton-card {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--radius); padding: 20px;
  box-shadow: var(--shadow);
}
.skeleton-line {
  height: 14px; border-radius: 6px; margin-bottom: 10px;
  background: linear-gradient(90deg, var(--border) 25%, var(--bg) 50%, var(--border) 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}
@keyframes shimmer { to { background-position: -200% 0; } }
.skeleton-line.sl-title { height: 20px; width: 60%; }
.skeleton-line.sl-short { width: 40%; }
.skeleton-line.sl-full  { width: 100%; }

/* Responsive improvements */
@media (max-width: 900px) {
  .stu-summary-grid { grid-template-columns: repeat(2, 1fr); }
  .kpi-grid { grid-template-columns: repeat(2, 1fr); }
  .admin-quick-stats { grid-template-columns: 1fr 1fr; }
  .dash-grid { grid-template-columns: 1fr; }
}
@media (max-width: 640px) {
  .stu-summary-grid { grid-template-columns: 1fr 1fr; }
  .schedule-grid { grid-template-columns: 1fr; }
  .lec-main, .stu-main { padding: 20px 16px; }
  .stu-profile-grid { grid-template-columns: 1fr; }
  .scv2-body { gap: 16px; }
  .daily-summary { gap: 8px; }
  .ds-card { min-width: 120px; }
}
/* ===================================
   NEW FEATURE STYLES
=================================== */

/* Leave Request Type Buttons */
.leave-type-btn {
  display: flex; align-items: center; justify-content: center;
  padding: 10px 8px;
  border: 1.5px solid var(--border);
  border-radius: var(--radius-xs);
  cursor: pointer;
  font-size: 13px; font-weight: 500;
  color: var(--text-muted);
  background: var(--bg);
  transition: all .2s;
  gap: 6px;
}
.leave-type-btn input[type="radio"] { display: none; }
.leave-type-btn:has(input:checked),
.leave-type-btn.selected {
  border-color: var(--warning);
  background: var(--warning-bg);
  color: var(--warning);
}
.leave-type-btn:hover { border-color: var(--border-strong); }

/* Leave Alert Banner */
.leave-alert {
  padding: 12px 16px;
  background: var(--warning-bg);
  border: 1px solid var(--warning-border);
  border-radius: var(--radius-sm);
  font-size: 13px;
  color: var(--warning);
  margin-bottom: 16px;
  display: flex; align-items: center; gap: 10px;
}

/* Lecturer Leave Request Button */
.checkin-btn.leave-btn {
  background: var(--warning-bg);
  color: var(--warning);
  border: 1.5px solid var(--warning-border);
}
.checkin-btn.leave-btn:hover:not(:disabled) {
  background: var(--warning);
  color: #fff;
}

/* Live Badge */
.live-badge {
  display: inline-block;
  font-size: 11px; font-weight: 600;
  color: var(--danger);
  animation: pulseLive 2s ease-in-out infinite;
}
@keyframes pulseLive {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.4; }
}

/* Notification Panel */
.notif-panel-backdrop {
  position: fixed; inset: 0;
  background: rgba(0,0,0,.3);
  z-index: 900;
  display: none;
  backdrop-filter: blur(1px);
}
.notif-panel-backdrop.active { display: block; }
.notif-panel {
  position: fixed;
  top: 0; right: -380px;
  width: 360px; height: 100vh;
  background: var(--surface);
  box-shadow: var(--shadow-xl);
  z-index: 901;
  transition: right .3s cubic-bezier(.4,0,.2,1);
  display: flex; flex-direction: column;
  border-left: 1px solid var(--border);
}
.notif-panel.open { right: 0; }
.notif-panel-header {
  display: flex; align-items: center; justify-content: space-between;
  padding: 20px 20px 16px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}
.notif-panel-header h3 { font-size: 16px; font-weight: 600; }
.notif-panel-body { flex: 1; overflow-y: auto; padding: 8px 0; }
.notif-item {
  display: flex; gap: 12px;
  padding: 12px 20px;
  border-bottom: 1px solid var(--border);
  cursor: pointer;
  transition: background .15s;
  position: relative;
}
.notif-item:hover { background: var(--bg); }
.notif-item.unread { background: var(--blue-light); }
.notif-item.unread::before {
  content: '';
  position: absolute;
  left: 8px; top: 50%; transform: translateY(-50%);
  width: 6px; height: 6px;
  border-radius: 50%;
  background: var(--blue);
}
.notif-icon {
  width: 36px; height: 36px; flex-shrink: 0;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  font-size: 16px;
}
.notif-icon.ni-warning { background: var(--warning-bg); }
.notif-icon.ni-info { background: var(--blue-light); }
.notif-icon.ni-success { background: var(--success-bg); }
.notif-icon.ni-danger { background: var(--danger-bg); }
.notif-content { flex: 1; min-width: 0; }
.notif-title { font-size: 13px; font-weight: 500; color: var(--text); line-height: 1.4; }
.notif-body { font-size: 12px; color: var(--text-muted); margin-top: 2px; line-height: 1.4; }
.notif-time { font-size: 11px; color: var(--text-faint); margin-top: 4px; }
.notif-empty { padding: 40px 20px; text-align: center; color: var(--text-muted); font-size: 14px; }
.notif-empty svg { margin-bottom: 12px; opacity: .4; }

/* Notification count badge on bell */
.nav-notif { position: relative; }
.nav-notif-count {
  position: absolute;
  top: -4px; right: -4px;
  width: 18px; height: 18px;
  background: var(--danger);
  color: #fff;
  font-size: 10px; font-weight: 700;
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  line-height: 1;
}

/* Approval Cards */
.approval-card {
  background: var(--surface);
  border: 1.5px solid var(--border);
  border-radius: 12px;
  padding: 24px;
  box-shadow: 0 2px 8px rgba(0,0,0,.04);
  transition: all .2s cubic-bezier(.4,0,.2,1);
}
.approval-card:hover { box-shadow: 0 8px 20px rgba(0,0,0,.08); }
.approval-card.pending { border-left: 5px solid var(--warning); }
.approval-card.approved { border-left: 5px solid var(--success); }
.approval-card.rejected { border-left: 5px solid var(--danger); }
.approval-header { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 16px; gap: 12px; }
.approval-title { font-size: 16px; font-weight: 700; color: var(--text); }
.approval-sub { font-size: 13px; color: var(--text-muted); margin-top: 3px; }
.approval-meta { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 12px; margin: 16px 0; padding: 16px 0; border-top: 1.5px solid var(--border); border-bottom: 1.5px solid var(--border); }
.approval-field label { font-size: 10px; font-weight: 700; text-transform: uppercase; color: var(--text-faint); letter-spacing: .06em; display: block; margin-bottom: 4px; }
.approval-field span { font-size: 14px; font-weight: 600; color: var(--text); }
.approval-actions { display: flex; gap: 10px; flex-wrap: wrap; margin-top: 16px; align-items: center; }
.approval-note { font-size: 13px; color: var(--text-muted); flex: 1; }
.btn-approve {
  padding: 10px 18px; border: none; border-radius: 8px;
  background: var(--success); color: #fff; font-size: 13px; font-weight: 600;
  cursor: pointer; transition: all .2s; box-shadow: 0 2px 8px rgba(22, 163, 74, .25);
}
.btn-approve:hover { background: #15803d; transform: translateY(-1px); box-shadow: 0 4px 12px rgba(22, 163, 74, .35); }
.btn-reject {
  padding: 10px 18px; border: 1.5px solid var(--danger); border-radius: 8px;
  background: transparent; color: var(--danger); font-size: 13px; font-weight: 600;
  cursor: pointer; transition: all .2s;
}
.btn-reject:hover { background: var(--danger); color: #fff; transform: translateY(-1px); }

/* Status chip extras */
.chip.leave { background: #fef3c7; color: #92400e; border: 1px solid #fde68a; }
.chip.online { background: #ede9fe; color: #6d28d9; border: 1px solid #ddd6fe; }
.chip.reschedule { background: #e0f2fe; color: #0369a1; border: 1px solid #bae6fd; }
.chip.cancelled { background: var(--danger-bg); color: var(--danger); border: 1px solid var(--danger-border); }
.chip.stu-late { background: #fff7ed; color: #c2410c; border: 1px solid #fed7aa; }

/* Status chips for lecturer class status */
.chip.cls-present { background: var(--success-bg); color: var(--success); border: 1px solid var(--success-border); }
.chip.cls-late { background: #fff7ed; color: #c2410c; border: 1px solid #fed7aa; }
.chip.cls-absent { background: var(--danger-bg); color: var(--danger); border: 1px solid var(--danger-border); }
.chip.cls-pending { background: var(--bg); color: var(--text-muted); border: 1px solid var(--border); }
.chip.cls-online { background: #ede9fe; color: #6d28d9; border: 1px solid #ddd6fe; }
.chip.cls-leave { background: #fef3c7; color: #92400e; border: 1px solid #fde68a; }
.chip.cls-cancelled { background: var(--danger-bg); color: var(--danger); border: 1px solid var(--danger-border); }
.chip.cls-reschedule { background: #e0f2fe; color: #0369a1; border: 1px solid #bae6fd; }

/* Audit Log */
.audit-item {
  display: flex; align-items: flex-start; gap: 12px;
  padding: 12px 20px;
  border-bottom: 1px solid var(--border);
  font-size: 13px;
}
.audit-item:last-child { border-bottom: none; }
.audit-dot {
  width: 8px; height: 8px; border-radius: 50%;
  margin-top: 5px; flex-shrink: 0;
}
.audit-dot.ad-checkin { background: var(--success); }
.audit-dot.ad-checkout { background: var(--blue); }
.audit-dot.ad-leave { background: var(--warning); }
.audit-dot.ad-approval { background: #7c3aed; }
.audit-dot.ad-settings { background: var(--text-faint); }
.audit-dot.ad-export { background: var(--blue); }
.audit-action { color: var(--text); line-height: 1.5; flex: 1; }
.audit-action strong { font-weight: 600; }
.audit-ts { font-size: 11px; color: var(--text-faint); white-space: nowrap; }

/* Pending approval badge in sidebar */
.sidebar-badge.pending { background: var(--warning); }

/* Lecturer schedule today view */
.lec-schedule-slot {
  padding: 14px 0;
  border-bottom: 1px solid var(--border);
  display: flex; align-items: flex-start; gap: 16px;
}
.lec-schedule-slot:last-child { border-bottom: none; }
.lec-slot-time { font-family: 'DM Mono', monospace; font-size: 13px; color: var(--text-muted); min-width: 110px; }
.lec-slot-detail { flex: 1; }
.lec-slot-name { font-weight: 600; font-size: 14px; margin-bottom: 2px; }
.lec-slot-meta { font-size: 12px; color: var(--text-muted); }

/* My Leave Requests list */
.my-leave-item {
  padding: 14px 0; border-bottom: 1px solid var(--border);
  display: flex; align-items: flex-start; gap: 14px;
}
.my-leave-item:last-child { border-bottom: none; }
.my-leave-type-icon {
  width: 38px; height: 38px; border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 18px; flex-shrink: 0;
  background: var(--bg);
}
.my-leave-detail { flex: 1; }
.my-leave-title { font-weight: 600; font-size: 14px; }
.my-leave-meta { font-size: 12px; color: var(--text-muted); margin-top: 2px; }
.my-leave-status { flex-shrink: 0; }

/* QR/GPS validation modal styles */
.validate-mode-card {
  padding: 20px;
  background: var(--bg);
  border-radius: var(--radius);
  text-align: center;
  margin-bottom: 16px;
}
.validate-mode-card svg { width: 48px; height: 48px; color: var(--blue); margin-bottom: 12px; }
.validate-mode-card h4 { font-size: 15px; font-weight: 600; margin-bottom: 8px; }
.validate-mode-card p { font-size: 13px; color: var(--text-muted); margin-bottom: 16px; }

/* Dark mode extras */
[data-theme="dark"] .approval-card { background: var(--surface); }
[data-theme="dark"] .notif-item.unread { background: rgba(29,78,216,.12); }

/* Admin approvals filter bar spacing */
#section-approvals .filters-bar { margin-bottom: 16px; }

/* ===================================
   GPS AUTO CHECK-IN PANEL
=================================== */
.gps-panel {
  background: var(--surface);
  border: 1.5px solid var(--blue-mid);
  border-radius: var(--radius);
  box-shadow: var(--shadow), 0 0 0 3px rgba(29,78,216,.04);
  overflow: hidden;
  margin-bottom: 16px;
  position: relative;
}
.gps-panel::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 3px;
  background: linear-gradient(90deg, var(--blue) 0%, #7c3aed 60%, #06b6d4 100%);
  border-radius: var(--radius) var(--radius) 0 0;
}

.gps-panel-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 20px 12px;
  border-bottom: 1px solid var(--border);
}
.gps-panel-title {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 14px;
  font-weight: 700;
  color: var(--text);
  letter-spacing: -.01em;
}
.gps-panel-title svg {
  color: var(--blue);
  flex-shrink: 0;
}

.gps-dist-badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 3px 10px;
  border-radius: 20px;
  font-size: 12px;
  font-weight: 600;
  background: var(--success-bg);
  border: 1px solid var(--success-border);
  font-family: 'DM Mono', monospace;
  transition: all .3s;
}

.gps-panel-body {
  padding: 16px 20px;
  display: flex;
  align-items: center;
  gap: 16px;
  flex-wrap: wrap;
}

.gps-status-line {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 8px;
  min-width: 160px;
}

/* GPS status dot states */
.gps-status-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--text-faint);
  flex-shrink: 0;
  transition: background .3s;
}
.gps-dot-green  { background: var(--success); }
.gps-dot-red    { background: var(--danger); }
.gps-dot-yellow { background: var(--warning); }

/* Pulsing animation while locating */
.gps-dot-pulse {
  background: var(--blue);
  box-shadow: 0 0 0 0 rgba(29,78,216,.5);
  animation: gpsPulse 1.4s ease-out infinite;
}
@keyframes gpsPulse {
  0%   { box-shadow: 0 0 0 0 rgba(29,78,216,.5); }
  70%  { box-shadow: 0 0 0 8px rgba(29,78,216,0); }
  100% { box-shadow: 0 0 0 0 rgba(29,78,216,0); }
}

/* Locating spinner text animation */
.gps-locating-text {
  color: var(--blue);
  font-size: 13px;
  animation: gpsLocatingFade 1.6s ease-in-out infinite;
}
@keyframes gpsLocatingFade {
  0%,100% { opacity: 1; }
  50%     { opacity: .5; }
}

/* GPS check-in button */
.gps-checkin-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  background: linear-gradient(135deg, var(--blue) 0%, #1e40af 100%);
  color: #fff;
  border: none;
  border-radius: var(--radius-sm);
  font-family: 'DM Sans', sans-serif;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all .2s cubic-bezier(.4,0,.2,1);
  box-shadow: 0 2px 8px rgba(29,78,216,.3);
  white-space: nowrap;
  letter-spacing: -.01em;
  flex-shrink: 0;
}
.gps-checkin-btn svg { width: 15px; height: 15px; }
.gps-checkin-btn:hover:not(:disabled) {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(29,78,216,.4);
  background: linear-gradient(135deg, #1e40af 0%, #1e3a8a 100%);
}
.gps-checkin-btn:active:not(:disabled) { transform: translateY(0); }
.gps-checkin-btn:disabled {
  background: linear-gradient(135deg, #93c5fd 0%, #6ea8fe 100%);
  cursor: not-allowed;
  transform: none;
  box-shadow: none;
  opacity: .75;
}
.gps-checkin-btn:focus { outline: 2px solid var(--blue); outline-offset: 3px; }

.gps-panel-footer {
  padding: 8px 20px 12px;
  font-size: 12px;
  color: var(--text-faint);
  display: flex;
  align-items: center;
  gap: 6px;
}
.gps-panel-footer strong { color: var(--blue); font-weight: 600; }
.gps-panel-footer svg { flex-shrink: 0; color: var(--blue); }